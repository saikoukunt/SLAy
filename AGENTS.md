- Do not write comments that describe what the code does -- this should already be clear via descriptive function/variables names. Only write comments that attribute code copied from elsewhere, explain the reasoning for a non-obvious design choice, or explain unconventional/complicated logic (e.g. a certain math shortcut, less readable code snippets to optimize a performance bottleneck, etc.)